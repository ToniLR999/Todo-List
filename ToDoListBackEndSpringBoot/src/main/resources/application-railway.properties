# Configuración para Railway
spring.profiles.active=railway

# Base de datos MySQL (Railway lo configurará automáticamente)
# Railway proporciona DATABASE_URL automáticamente

# Configuración de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.use_sql_comments=false

# Configuración de seguridad
spring.security.user.name=${RAILWAY_USERNAME:admin}
spring.security.user.password=${RAILWAY_PASSWORD:admin}

# Configuración de logging
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=ERROR
logging.level.org.springframework.security=INFO

# Configuración de puerto
server.port=${PORT:8080}

# Configuración de CORS para Railway
spring.web.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://your-frontend-url.netlify.app,http://localhost:4200}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Configuración de email (usar variables de entorno)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:grodt.devmail@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ooxviqbjfbneeraa}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# JWT Configuration (usar nombres que coincidan con el código)
app.jwtSecret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwtExpirationInMs=${JWT_EXPIRATION:86400000}

# Redis Configuration (usar variables de entorno)
spring.data.redis.host=${REDIS_HOST:redis-18892.c15.us-east-1-4.ec2.redns.redis-cloud.com}
spring.data.redis.port=${REDIS_PORT:18892}
spring.data.redis.password=${REDIS_PASSWORD:PvIV5PLObG2O2GdZ01MMzPP4xrvuFIfE}
spring.data.redis.ssl.enabled=false
spring.data.redis.timeout=60000

# Cache Configuration
spring.cache.type=redis
spring.cache.redis.time-to-live=600000
spring.cache.redis.key-prefix=todolist:
spring.cache.redis.use-key-prefix=true
spring.cache.redis.cache-null-values=false

# Redis Connection Pool
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0
spring.data.redis.lettuce.pool.max-wait=-1ms

# OpenAPI/Swagger Configuration
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.disable-swagger-default-url=true
springdoc.swagger-ui.config-url=/api-docs/swagger-config
springdoc.swagger-ui.url=/api-docs 